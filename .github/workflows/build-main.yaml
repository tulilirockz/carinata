name: Build Carinata

permissions:
  contents: read
  packages: write
  id-token: write

on:
  push:
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 1 * * TUE"
  merge_group:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/reusable-build-bootc.yaml
    secrets: inherit
    with:
      image-name: carinata
      platforms: amd64,arm64
      image-description: "Bootc image for usage on Lima"
      image-license: "Apache-2.0"
      image-keywords: "bootc-lima"
      profiles: docker,bootc-ostree,vm-base,carinata-bootc-ostree
      publish: ${{ github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
